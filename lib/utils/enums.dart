enum Gender { male, female, none }

enum Feeling { happy, sad, angry, none }

enum Status { suspended, deactivated, active, inactive, incomplete }

enum ChatType { text, image, video, audio }

enum ChatStatus { delivered, seen, deleted }
